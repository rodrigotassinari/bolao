%h2 Editar sua conta

- form_for resource_name, resource, :url => registration_path(resource_name), :html => { :method => :put } do |f|
  = f.error_messages
  
  %p= f.label :name, 'Nome'
  %p= f.text_field :name
  
  %p= f.label :email, 'Email'
  %p= f.text_field :email

  %p
    = f.label :password, 'Senha'
    %i (deixe em branco caso não deseje alterá-la)
  %p= f.password_field :password

  %p= f.label :password_confirmation, 'Senha (confirmação)'
  %p= f.password_field :password_confirmation

  %p
    = f.label :current_password, 'Senha Atual'
    %i (precisamos da sua senha atual para confirmar as alterações)
  %p= f.password_field :current_password

  %p= f.submit "Enviar"

%h3 Cancelar minha conta

%p Insatisfeito? #{link_to "Cancele sua conta", registration_path(resource_name), :confirm => "Tem certeza?", :method => :delete}.

= render :partial => "shared/devise_links"

